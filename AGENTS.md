# AGENTS.md — Working Rules

## Mandatory
- You have FULL read access to this repository. Read every file yourself using
  read_file / grep / glob / search. NEVER ask the user to send, paste, or
  upload files.
- If a file or context is unclear: search deeper, follow imports, check
  migrations/schema/tests first. Investigate before asking.
- Only ask the user for DECISIONS (preferences, tradeoffs), never for files.
- If something is genuinely missing, note it and proceed with what exists.
- Never invent facts (paths, tables, schemas). Verify from source.
- For changes: explain the plan, then implement. Follow existing conventions.
